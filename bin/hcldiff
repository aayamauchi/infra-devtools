#!/bin/bash

trap 'rm -rf ${TMP_DIR}' EXIT

TMP_DIR=$( mktemp -d )

refmt -t yaml ${1} ${TMP_DIR}/${1}.yaml
refmt -t yaml ${2} ${TMP_DIR}/${2}.yaml

yamldiff ${TMP_DIR}/${1}.yaml ${TMP_DIR}/${2}.yaml
